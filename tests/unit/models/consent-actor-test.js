import { moduleForModel, test } from 'ember-qunit';

moduleForModel('consent-actor', 'Unit | Model | Consent_Actor', {
  needs: [
    'model:meta',
    'model:narrative',
    'model:resource',
    'model:extension',
    'model:codeable-concept',
    'model:reference'
  ]
});

test('it exists', function(assert) {
  const model = this.subject();
  assert.ok(!!model);
});